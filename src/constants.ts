export enum Routes {
  Login = '/login',
  PasswordHealth = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Root = '/',
}

export enum API {
  Login = 'api/login',
  Logout = 'api/logout',
  Items = 'api/items',
  User = 'api/user',
}

export const UNAUTHORIZED_STATUS = 401;
